# SQL_Table_Column_Search
simple query to search columns or columns with particular tables 
Inserting all the base tables and columns of that table in temp table and query the table (simple , brute force)
